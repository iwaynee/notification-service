version: '3.3'
services:
  app:
    container_name: notification_service
    build: .
    volumes:
      - /usr/app/node_modules
    environment:
      - TWILIO_SID
      - TWILIO_TOKEN
      - MAIL_MAIL
      - MAIL_PASSWORD
    ports:
      - '3010:3010'
