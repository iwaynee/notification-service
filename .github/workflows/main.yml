name: Node.js CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: self-hosted
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

#      - name: Install dependencies
#        run: npm install

#      - name: Build Code
#        run: npm run build

      - name: Docker
        run: |
          docker-compose down
          docker-compose build
          docker-compose up -d
